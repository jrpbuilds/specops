/** @deprecated Import V1 manifest APIs from `agents/manifest` directly. */
export { DEFAULT_MANIFEST, manifestAgentConfig, validateManifest } from "./agents/manifest.js";
/** @deprecated Import V1 manifest types from `agents/manifest` directly. */
export type { ManifestEntry, SpecOpsManifest } from "./agents/manifest.js";
